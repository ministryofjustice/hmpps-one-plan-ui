{% extends "../partials/layout.njk" %}
{%- from "moj/components/badge/macro.njk" import mojBadge -%}

{% set pageTitle = applicationName + " - Database with UI" %}
{% set mainClasses = "app-container govuk-body" %}

{% block content %}
    <!--<img width=32 height=32 src="assets/images/profile.png">-->
    <h1 class="govuk-!-margin-bottom-0">CRN: 42378429378</h1>
    <hr>
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds">
            <p class="govuk-body govuk-hint">Plan: English</p>
        </div>
        <div class="govuk-grid-column-one-third">
            <p class="govuk-body govuk-hint">Added by: D. Jones (POM)</p>
            <p class="govuk-body govuk-hint">Added on: 2nd Fed 2024</p>
        </div>
    </div>


    <h2>Objectives</h2>

    {% for objective in objectives %}
        <div class="govuk-grid-row">
            <div class="govuk-grid-column-two-thirds">
                <h3>{{ objective.title }}</h3>
                {% if objective.steps.length > 0 %}
                <h4 class="steps-heading">Steps</h4>
                <ul>
                    {% for step in objective.steps %}
                        <li>
                            <div class="govuk-grid-row">
                                <div class="govuk-grid-column-two-thirds">{{ step.description }}</div>
                                <div class="govuk-grid-column-one-third">
                                    {{ mojBadge({
                                        text: step.status
                                    }) }}</div>
                            </div>
                        </li>
                    {% endfor %}
                </ul>
                {% endif %}
            </div>
            <div class="govuk-grid-column-one-third objective-audit">
                <p class="govuk-hint">Added by: {{ objective.createdBy }}</p>
                <p class="govuk-hint">Added on: {{ objective.createdAt }}</p>
            </div>
        </div>
    {% endfor %}



{% endblock %}
